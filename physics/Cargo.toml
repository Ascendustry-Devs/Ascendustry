[package]
name = "physics"
version.workspace = true
edition.workspace = true

[lib]
path = "lib.rs"

[features]
server = []
client = []

[dependencies]
cgmath = { workspace = true }
project_core = { path = "../project_core" }
game = { path = "../game" }
